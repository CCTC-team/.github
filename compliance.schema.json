{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/CCTC-team/.github/blob/main/compliance.schema.json",
  "title": "CCTC Compliance Metadata",
  "description": "Machine-readable regulatory metadata for a CCTC repository. Authoritative source: CCTC-team/.github.",
  "type": "object",
  "required": ["schema_version", "regulatory_tier", "regulatory_pillars", "gamp_category", "last_reviewed"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "description": "Schema version of this metadata. An integer >= 1; the validator workflow declares which versions it accepts via its supported_schema_versions input. The current schema is version 1 — the single initial version, which already includes validated_paths/exempt_paths and ctq_factors/governing_documents. Bumped only on a future breaking change.",
      "type": "integer",
      "minimum": 1
    },
    "regulatory_tier": {
      "description": "Inspection bucket and validation rigour. Must match the org custom property `regulatory_tier` set on this repo.",
      "enum": ["gcp-critical", "gcp-supporting", "data-protection", "none"]
    },
    "regulatory_pillars": {
      "description": "Which regulatory clusters apply to this repo. Each pillar bundles a set of related regulations — the granular citation register lives in the CTU's QMS / SOPs, not here. An inspector wants to see that you considered each pillar (including the ones you ruled out), not that you enumerated every paragraph of every regulation.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "required": ["pillar", "status"],
        "additionalProperties": false,
        "properties": {
          "pillar": {
            "description": "Allowed pillars and what they bundle:\n- uk-statutory — UK law baseline: UK CT Regs 2004 (as amended by SI 2025/538), UK-GDPR, DPA 2018, NHS-DSPT, HRA Transparency, CAG-S251 (conditional on unconsented PID). Mandatory for any UK trial work.\n- mhra-csv-di — MHRA audit focus: ICH E6 (R3), MHRA GxP Data Integrity Guidance, MHRA Inspectorate CSV-for-GCP expectations. How the system is built and validated.\n- infra-security — Hosting environment: Cyber Essentials Plus, ISO 27001. Frequently a contractual gate for NIHR/UKRI-funded work.\n- international-ehr-standards — 21 CFR Part 11 and EU Annex 11. Applies only with FDA/EMA submission exposure; out-of-scope for UK-only academic trials.\n- eu-ct-regulation — EU CTR 536/2014. Applies only when the trial has active EU sites (CTIS-routed since Jan 2025).\n- samd-aimd — MHRA Software/AI as Medical Device. Applies only when the system makes or guides clinical decisions (e.g. dose-adjustment algorithms). A pure data-capture EDC is not SaMD.",
            "enum": [
              "uk-statutory",
              "mhra-csv-di",
              "infra-security",
              "international-ehr-standards",
              "eu-ct-regulation",
              "samd-aimd"
            ]
          },
          "status": {
            "enum": ["in-scope", "out-of-scope", "partial"]
          },
          "notes": {
            "description": "Optional rationale or scoping detail. Useful when status is `out-of-scope` (so an inspector can see you considered and ruled out) or `partial` (which sub-areas are in scope).",
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "gamp_category": {
      "description": "GAMP 5 software category. 3 = off-the-shelf unconfigured, 4 = configured (e.g. RAVE/REDCap as deployed), 5 = custom code (in-house apps, scripts, EMs).",
      "enum": [3, 4, 5]
    },
    "retention_years": {
      "description": "Minimum retention period in years for data/code held by this system. UK CT Regs (as amended by SI 2025/538, in force 28 April 2026) require 25 years for trial records on applications submitted on or after that date; pre-amendment trials remain on the 5-year rule under the 2004 Regs.",
      "type": "integer",
      "minimum": 1,
      "maximum": 50
    },
    "audit_trail_kind": {
      "description": "Inspector-readiness: what kind of audit trail this system provides. MHRA Inspectorate expects extractable, immutable trails for gcp-critical systems.",
      "enum": ["extractable", "read-only", "append-only", "none"]
    },
    "account_model": {
      "description": "Inspector-readiness: are user accounts individually attributable? MHRA position is that shared accounts break ALCOA+ attributability for any GCP-relevant action.",
      "enum": ["named", "named-with-roles", "shared", "mixed"]
    },
    "pid_boundary": {
      "description": "Inspector-readiness: where is patient-identifiable data held relative to this system? `separated` = system holds only pseudonymised/coded data; `commingled` = system holds direct PID; `none` = system handles no PID at all.",
      "enum": ["separated", "commingled", "none"]
    },
    "csv_evidence": {
      "description": "Inspector-readiness: URL or repo path to the Computer System Validation evidence pack (URS/FS/IQ/OQ/PQ + risk assessment). Critical-trial GAMP 5 systems must be able to point at this on demand.",
      "type": "string",
      "minLength": 1
    },
    "requirements": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "audit_trail": { "enum": ["required", "recommended", "not-applicable"] },
        "electronic_signatures": { "enum": ["required", "recommended", "not-applicable"] },
        "validation_documentation": { "enum": ["required", "recommended", "not-applicable"] },
        "change_control": { "enum": ["formal", "lightweight", "not-applicable"] },
        "access_review_frequency": { "enum": ["monthly", "quarterly", "biannual", "annual"] }
      }
    },
    "contacts": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "qa_lead": { "type": "string", "minLength": 1 },
        "data_protection_officer": { "type": "string", "minLength": 1 },
        "sponsor_contact": { "type": "string", "minLength": 1 }
      }
    },
    "last_reviewed": {
      "description": "ISO date of the last formal review of this metadata.",
      "type": "string",
      "format": "date"
    },
    "review_cadence_months": {
      "description": "Maximum interval between reviews, in months.",
      "type": "integer",
      "minimum": 1,
      "maximum": 24
    },
    "validated_paths": {
      "description": "Glob patterns (gitignore-style) for paths inside this repo that constitute the validated state. The traceability gate workflow enforces Risk ID + Requirement ID linkage on PRs that change any matching file. If omitted, the gate defaults to strict mode (every PR gated).",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "exempt_paths": {
      "description": "Glob patterns for files that sit inside a validated_paths match but are not themselves validated artefacts (typically docs, READMEs, comments-only files). Subtracted from the validated set when computing whether a PR triggers the gate.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "ctq_factors": {
      "description": "Critical-to-Quality factors (CCTU/FRM129) this system supports or safeguards. ICH E6(R3) Principle 6 treats fit-for-purpose validation of computerised systems handling clinical data/endpoints as a CtQ factor; this anchors the system to its FRM129 entry so an inspector can trace CtQ factor -> risk -> requirement -> V&V. Required for gcp-critical.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "required": ["frm129_ref", "tier"],
        "additionalProperties": false,
        "properties": {
          "frm129_ref": { "type": "string", "minLength": 1, "description": "CtQ factor identifier on CCTU/FRM129 (trial-specific form ID + row, e.g. FRM129-XYZ-001#3)." },
          "tier": { "enum": ["critical", "important"], "description": "FRM129 classification. critical = compromise breaks participant safety / result reliability; important = significant but not critical. 'neither' factors are not listed." },
          "notes": { "type": "string", "minLength": 1 }
        }
      }
    },
    "governing_documents": {
      "description": "QMS documents (SOPs, guidance, forms, templates) governing this system's validation and risk management — the pointer layer between repo controls and the CTU QMS. The granular regulation register stays in the QMS. Required for gcp-critical.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "required": ["ref", "role"],
        "additionalProperties": false,
        "properties": {
          "ref": { "type": "string", "minLength": 1, "description": "QMS document number, e.g. CCTU/SOP040, CCTU/GD073, CCTU/FRM129." },
          "title": { "type": "string", "minLength": 1 },
          "role": { "enum": ["risk-assessment", "ctq-identification", "csv", "monitoring", "change-control", "other"] }
        }
      }
    }
  },
  "allOf": [
    {
      "if": { "properties": { "regulatory_tier": { "const": "gcp-critical" } } },
      "then": {
        "properties": {
          "gamp_category": { "enum": [4, 5] },
          "requirements": {
            "required": ["audit_trail", "validation_documentation", "change_control"]
          }
        },
        "required": ["requirements", "retention_years", "audit_trail_kind", "account_model", "pid_boundary", "ctq_factors", "governing_documents"]
      }
    },
    {
      "if": { "properties": { "regulatory_tier": { "const": "gcp-supporting" } } },
      "then": {
        "required": ["retention_years"]
      }
    }
  ]
}
